name: LyttleGravestone
version: '${project.version}'
main: be.thatsatim.gravestone.LyttleGravestone
api-version: '1.20'
author: ThatsATim

dependencies:
  server:
    Vault:
      load: BEFORE
      required: true
      join-classpath: true